
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "event-contracts"
version = "0.1.0"
description = "Kafka event schemas and envelopes"
dependencies = ["pydantic>=2.0", "confluent-kafka>=2.12"]

[tool.setuptools.packages.find]
where = ["src"]
